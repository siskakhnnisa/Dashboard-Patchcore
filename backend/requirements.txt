# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9      # untuk file upload

# WebSocket
websockets==12.0

# ML & Computer Vision
torch==2.3.0
torchvision==0.18.0
opencv-python==4.9.0.80
numpy==1.26.4
scikit-learn==1.4.2          # PatchCore biasanya pakai sklearn
scipy>=1.13.0                # morphological ops (ndimage.label, gaussian_laplace)
Pillow==10.3.0
onnxruntime>=1.17.0          # EfficientNet-B3 ONNX classifier (trial2)
segmentation-models-pytorch>=0.3.3  # U-Net runway segmentation
faiss-cpu                    # 10-100x faster kNN scoring (PatchCore)
decord>=0.6.0                # 2-3x faster video decoding (GPU-capable)
av>=12.0.0                   # PyAV — fast RTSP/RTMP stream decoding
prometheus-client>=0.21      # Prometheus metrics endpoint
orjson>=3.10                 # 3-10x faster JSON encode/decode

# Database
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.22.0            # async SQLite driver
alembic>=1.13                # database migrations

# Utilities
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1
aiofiles==23.2.1             # async file I/O
loguru==0.7.2                # logging yang lebih baik

# CORS & Security
starlette==0.37.2